

<h1>Add Raids through Existing Gyms</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gym_infos.each do |gym_info| %>
      <tr>
        <td><%= gym_info.name %></td>
        <td><%= gym_info.address %></td>
        <td><%= gym_info.latitude %></td>
        <td><%= gym_info.longitude %></td>
        <td><%= link_to 'Add Raid', new_user_gym_info_raid_path(:gym_info_id => gym_info.id) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

Add Gyms with: <%= link_to 'GeoLocation', new_gym_info_path %> or Manually
<br/>
<%= link_to 'Go to Dashboard', dashboard_index_path %>
