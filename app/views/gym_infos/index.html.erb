

<h1>Gym Infos</h1>

<table>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Latitude</th>
      <th>Longitude</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @gym_infos.each do |gym_info| %>
      <tr>
        <td><%= gym_info.name %></td>
        <td><%= gym_info.address %></td>
        <td><%= gym_info.latitude %></td>
        <td><%= gym_info.longitude %></td>
        <td><%= link_to 'Show', gym_info %></td>
        <td><%= link_to 'Edit', edit_gym_info_path(gym_info) %></td>
        <td><%= link_to 'Destroy', gym_info, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

Add Gyms with: <%= link_to 'GeoLocation', new_gym_info_path %> or Manually
<br/>
<%= link_to 'Go to Dashboard', dashboard_index_path %>
